\documentclass[manuscript,anonymous,review]{acmart}
\usepackage{amsmath}

\AtBeginDocument{%
  \providecommand\BibTeX{{\normalfont B\kern-0.5em{\scshape i\kern-0.25em b}\kern-0.25em T\kern-0.1667em\lower0.7ex\hbox{E}\kern-0.125emX}}}

% ---------------------------------------------------------------------------
% CCS concepts (required for articles > 2 pages, JDIQ_GUIDELINE_SUMMARY.md Sec.4)
% ---------------------------------------------------------------------------
\begin{CCSXML}
<ccs2012>
   <concept>
       <concept_id>10002951.10003317.10003347.10003348</concept_id>
       <concept_desc>Information systems~Data cleaning</concept_desc>
       <concept_significance>500</concept_significance>
       </concept>
   <concept>
       <concept_id>10002951.10003317.10003347</concept_id>
       <concept_desc>Information systems~Data quality</concept_desc>
       <concept_significance>500</concept_significance>
       </concept>
   <concept>
       <concept_id>10002951.10003227.10003236</concept_id>
       <concept_desc>Information systems~Retrieval models and ranking</concept_desc>
       <concept_significance>500</concept_significance>
       </concept>
   <concept>
       <concept_id>10002951.10003317.10003359</concept_id>
       <concept_desc>Information systems~Data cleaning</concept_desc>
       <concept_significance>300</concept_significance>
       </concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Information systems~Data cleaning}
\ccsdesc[500]{Information systems~Data quality}
\ccsdesc[500]{Information systems~Retrieval models and ranking}

\keywords{preference graphs, data quality, structural inconsistency, rank aggregation,
  feedback arc set repair, retrieval evaluation, benchmark curation}

\title{Score Calibration Governs Preference-Graph Repair Outcomes in
  Multi-Ranker Retrieval}

\author{Anonymous Author(s)}
\affiliation{%
  \institution{Institution redacted for review}
  \country{}
}

\begin{document}

\begin{abstract}
Preference-graph repair is often motivated by the idea that removing cyclic
inconsistency should improve downstream ranking quality, but that conclusion
depends on how the graph is constructed. We revisit this question on
SciDocs, FiQA, HotpotQA, and BRIGHT using the same stored BM25, TF-IDF, and
MiniLM candidate scores, candidate pools, qrels, repair code, and
evaluation pipeline as the original study while replacing its unstable
raw-margin weighting with a calibrated canonical protocol: per-query,
per-ranker min-max normalization followed by retention-matched vote and edge
thresholds. The raw implementation proved severely scale dominated: when
BM25 participated in an edge, its conditional mean weight share was
approximately 0.988, versus approximately 0.512 under the calibrated
primary protocol. Calibration materially changed retained votes, edge
weights, cyclicity, removed edges, and several repaired-versus-unrepaired
signs. Under the canonical protocol, repair still changes graph structure,
but structural consistency and retrieval effectiveness remain distinct:
repair can reduce graph inconsistency without producing a reliable
improvement in ranked retrieval. No repaired-versus-unrepaired improvement
remains robust after permutation testing, bootstrap uncertainty, influence
analysis, and multiplicity correction. Simple baselines such as reciprocal
rank fusion and CombSUM also remain strong comparators under the calibrated
graphs. The revised conclusion is therefore methodological as well as
empirical: score calibration and vote construction jointly determine the
preference graph on which repair operates, and claims about repair
effectiveness must report those choices explicitly rather than treating graph
construction as a neutral preprocessing step.
\end{abstract}

\maketitle

% =============================================================================
\section{Introduction}
\label{sec:introduction}

Modern retrieval pipelines rarely rely on a single ranking signal. Sparse
retrievers, dense retrievers, and other learned ranking components are often
treated as separate sources of evidence whose outputs must be reconciled
rather than trusted individually~\cite{liu2011learning,burges2005learning,dwork2001rank,ailon2008aggregating}.
Preference graphs offer a natural way to preserve that disagreement: for a
given query, a directed edge from document $u$ to document $v$ records
evidence that $u$ should outrank $v$, and the edge weight determines how
strongly that preference should influence the final ordering. This
representation is appealing because it makes conflict explicit instead of
collapsing it immediately into a single scalar score. But the graph is only
as meaningful as the procedure used to construct it. If heterogeneous ranker
scores are converted into edge weights without a defensible notion of
comparability, then the resulting graph can reflect score scale as much as
evidential quality.

That issue matters because preference-graph repair operates entirely on the
constructed graph. Once weighted pairwise evidence is aggregated, the graph
may contain directed cycles, so no single ranking can satisfy every recorded
preference. A standard response is to repair the graph by removing a
minimum-weight feedback arc set before extracting a final ranking~\cite{ailon2008aggregating,kenyon2007fewerrors}.
This intervention improves structural consistency relative to the graph's own
edge weights, but structural consistency and retrieval effectiveness are not
the same objective. A repair step can remove cycle-inducing edges, reduce a
graph-internal inconsistency measure, and still fail to improve a relevance
metric such as normalized discounted cumulative gain (nDCG) if the modified
edges do not alter the final ranking in a helpful way. For that reason, a
study of preference-graph repair must examine not only the repair algorithm
but also the graph-construction choices that determine what the repair sees.

This paper studies that upstream dependency directly. Rather than asking for
a predictive answer to ``when repair helps,'' we examine how score
calibration and vote construction govern the preference graph on which repair
operates, and how those choices affect both structural outcomes and
downstream retrieval behavior. We revisit the same four-benchmark setting as
the original study---SciDocs, FiQA, HotpotQA, and BRIGHT with stored BM25,
TF-IDF, and MiniLM score files~\cite{cohan2020scidocs,maia2018fiqa,yang2018hotpotqa,su2024bright}---but
replace its unstable raw-margin weighting with a calibrated canonical
protocol: per-query, per-ranker min-max normalization followed by
retention-matched thresholding. The motivation for this change is empirical,
not cosmetic. In the historical raw implementation, BM25 contributed nearly
all aggregate edge weight whenever it participated, so the graph was largely
BM25-weighted even when three rankers were nominally present. Treating such
raw heterogeneous margins as directly comparable support signals is not a
neutral preprocessing choice; it is a substantive graph-construction
assumption that shapes every downstream result.

Once graph construction is treated as a first-class variable, several parts
of the original narrative change. Calibration materially alters retained
votes, edge weights, cyclicity, removed edges, and some repaired-versus-unrepaired
signs. It also sharpens the distinction between total cyclicity and the
smaller amount of nontrivial cyclicity that remains after direct mutual pairs
are deleted. Repair therefore remains structurally active under the
calibrated graphs, but its retrieval effects are method-dependent and
calibration-sensitive rather than stable properties of ``repair'' in the
abstract. The central empirical result of the revised study is not that one
dataset provides a robust positive exception, nor that repair has a
calibration-independent null effect. It is that graph-construction choices
substantially determine both the structure being repaired and the retrieval
conclusions drawn from that repair.

This framing changes how the paper's contributions should be understood. We
do not present feedback-arc-set repair as a generally superior reranking
strategy, and we do not interpret raw-margin results as canonical evidence.
Instead, the paper contributes a calibrated evaluation protocol for
multi-ranker preference graphs, a structural analysis that separates mutual
pairs from longer-cycle behavior, a four-dataset reassessment of repaired
versus unrepaired retrieval effects under paired statistical testing and
robustness checks, and a raw-versus-calibrated ablation showing that some
historical signs are not stable under defensible graph reconstruction. These
contributions support a more limited but more defensible conclusion:
preference-graph repair outcomes are not calibration-invariant, and
structural consistency should not be assumed to translate into retrieval
improvement without direct evaluation.

The remainder of the paper is organized as follows. Section~2 positions the
study relative to data-quality frameworks, rank aggregation, and rank fusion.
Section~3 formalizes preference graphs, calibrated vote construction,
structural metrics, and feedback-arc-set repair. Section~4 describes the
four-dataset protocol, vote-extraction regimes, baselines, and statistical
evaluation policy. The results sections then report calibrated structural
behavior, repaired-versus-unrepaired retrieval effects, raw-versus-calibrated
ablations, and the implications of those findings for baseline comparison and
practical use. The final sections discuss limitations, reproducibility, and
the broader lesson that graph-construction choices must be reported
explicitly in any empirical claim about preference-graph repair.

% =============================================================================
\section{Background and Related Work}
\label{sec:background}

Preference-graph methods sit at the intersection of several literatures:
rank aggregation, noisy pairwise-comparison ranking, retrieval fusion, and
data quality for derived analytical artifacts. The revised goal of this paper
is narrower than a new ranking-method contribution. We do not introduce a new
feedback-arc-set solver, a new retrieval ranker, or a new fusion rule.
Instead, we study how heterogeneous ranker scores are converted into weighted
preference graphs and how those graph-construction choices govern the measured
effect of repair on both structural consistency and downstream retrieval.

\textbf{Rank aggregation and preference graphs.} Preference graphs are a
standard representation for aggregating pairwise evidence across voters,
rankers, or judges~\cite{dwork2001rank,ailon2008aggregating,negahban2017rankcentrality}.
For a query-specific candidate set, a directed edge $u \rightarrow v$ records
support for ranking document $u$ ahead of document $v$, while the edge weight
records the strength of that support. This representation is useful because it
preserves disagreement as explicit graph structure instead of collapsing it
immediately into a single fused score.

\textbf{Feedback arc set repair and Kemeny-style consistency.} Once a
preference graph contains a directed cycle, no total ranking can satisfy every
recorded preference simultaneously. Classical rank-aggregation work connects
this problem to minimum weighted feedback arc set (MWFAS) and Kemeny-style
objectives: remove or violate as little edge weight as possible to obtain an
acyclic ordering~\cite{dwork2001rank,ailon2008aggregating,kenyon2007fewerrors,fagin2003comparing}.
That optimization target is purely graph-internal. It formalizes
\emph{structural} consistency, not relevance to external retrieval judgments.
This distinction is central here: a better solution to the graph's own repair
objective need not improve a metric such as nDCG.

\textbf{Probabilistic ranking from noisy pairwise preferences.} Related
probabilistic formulations, such as Rank Centrality, model pairwise outcomes as
noisy evidence about a latent ordering and derive node scores from the induced
comparison graph~\cite{negahban2017rankcentrality}. Those methods are useful in
this paper as graph-dependent baselines, but they share the same upstream
dependence as feedback-arc-set repair: the statistical object they consume is
the constructed graph, so changing vote extraction or edge weighting can alter
their behavior even when the upstream retrieval runs are unchanged.

\textbf{Rank fusion in information retrieval.} Information-retrieval systems
already combine heterogeneous signals through score or rank fusion, including
reciprocal rank fusion (RRF), CombSUM, and Borda-style methods~\cite{cormack2009rrf,fox1994combination,dwork2001rank}.
These baselines matter here for two reasons. First, they provide graph-free
comparators against which repaired and unrepaired graph methods must be judged.
Second, they make explicit a design choice that preference-graph work can hide:
when heterogeneous retrieval scores are merged, some form of scale handling or
an explicit decision to ignore cross-model score comparability becomes part of
the method before score magnitudes are interpreted jointly.

\textbf{Score calibration for heterogeneous retrieval signals.} BM25, TF-IDF,
and neural similarity models operate on different native score scales. A raw
difference of $0.05$ has no reason to represent comparable evidential strength
across those models. The methodological issue studied in this paper is
therefore not whether preference graphs are useful in principle, but whether
weighted graph construction is stable when native heterogeneous score margins
are treated as if they were directly comparable. Our calibrated protocol makes
that assumption explicit and testable rather than leaving it implicit in the
edge weights.

\textbf{LLM pairwise ranking and inconsistency.} Recent work has used large
language models as retrieval judges, rerankers, and pairwise preference
sources~\cite{sun-etal-2023-chatgpt,qin-etal-2024-large}. That line of work
makes inconsistency salient, but it does not remove the measurement problem
studied here. Whether the pairwise evidence comes from classical rankers or
from LLM judgments, downstream claims about ``repair'' still depend on how
pairwise evidence is filtered, weighted, and aggregated into a graph.

\textbf{Derived-artifact data quality.} We treat the preference graph itself as
a derived data artifact whose properties can be audited. Cyclicity, strongly
connected components, mutual contradictions, and removed feedback-arc weight
are intrinsic properties of the graph produced by a construction pipeline,
whereas retrieval metrics such as nDCG are extrinsic properties of rankings
evaluated against qrels. The novelty of this paper lies in separating those
two measurement families and showing that conclusions about repair are not
calibration-invariant. In this sense, the paper is a study of graph
construction and measurement sensitivity, not a claim that a particular repair
heuristic solves retrieval reranking.

% =============================================================================
\section{Methodology}
\label{sec:methodology}

This section defines the calibrated preference-graph pipeline used in the
canonical evidence package. The order matters. Upstream score scale affects
pairwise margins; pairwise margins affect retained votes; retained votes
determine weighted graph edges; graph structure determines what repair sees;
and repair influences ranking extraction only through the modified graph.
Table~\ref{tab:notation} summarizes notation used in this section and
Section~\ref{sec:experimental-setup}.

\begin{table}[t]
  \caption{Notation used in Sections~\ref{sec:methodology}
    and~\ref{sec:experimental-setup}.}
  \label{tab:notation}
  \begin{tabular}{cl}
    \toprule
    Symbol & Meaning \\
    \midrule
    $q$, $D_q$ & Query and its candidate document set \\
    $s$ & Upstream ranker in $\{\mathrm{BM25},\mathrm{TF\mbox{-}IDF},\mathrm{MiniLM}\}$ \\
    $x_{q,s}(d)$ & Native score assigned by ranker $s$ to document $d$ for query $q$ \\
    $\bar{x}_{q,s}(d)$ & Calibrated per-query, per-ranker score \\
    $m_{q,s}(u,v)$ & Absolute pairwise margin for pair $\{u,v\}$ under ranker $s$ \\
    $G_q=(V_q,E_q,w_q)$ & Preference graph for query $q$ ($V_q = D_q$) \\
    $\widetilde G_q$ & Repaired (acyclic) preference graph \\
    $u \succ v$ & Pairwise preference: $u$ ranked ahead of $v$ \\
    $r$ & Vote-extraction regime, $r\in\{\texttt{ms2},\texttt{ms1},\texttt{ms1\_drop\_mutual}\}$ \\
    $\kappa_r$ & Regime-specific minimum support threshold \\
    $\lambda_s$ & Per-ranker vote-retention threshold within one dataset/protocol/regime cell \\
    $\gamma_r$ & Aggregate edge-retention threshold within one dataset/protocol/regime cell \\
    $c_q(u,v)$ & Support count for direction $u \rightarrow v$ \\
    $a_q(u,v)$ & Aggregate retained weight for direction $u \rightarrow v$ \\
    $F_{\mathrm{opt}}$ & Exact minimum-weight feedback arc set (optimization target) \\
    $F_{\mathrm{greedy}}$ & Greedy removed-edge set computed in the experiments \\
    $\deg^+(u),\deg^-(u)$ & Out-degree, in-degree of node $u$ in a graph \\
    $s_{\mathrm{cop}}, s_{\mathrm{bal}}, s_{\mathrm{mc}}$ & Copeland, weighted balance, and Markov graph scores \\
    $\hat{s}(\cdot)$ & Min--max normalization of a score vector to $[0,1]$ \\
    $\alpha$ & Hybrid mixing weight for the graph-derived component \\
    $\pi$ & A ranking (permutation) of $V_q$ \\
    $\mathrm{BEW}(\pi,G)$ & Backward-edge weight of $\pi$ relative to graph $G$ \\
    $\mathrm{PIC}(\pi,G)$ & Pairwise inconsistency count (unweighted analogue) \\
    $k$ & nDCG cutoff \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \fbox{\parbox{0.94\linewidth}{\small\centering
    \textbf{Stored ranker scores}
    $\rightarrow$
    \textbf{Per-query, per-ranker calibration}
    $\rightarrow$
    \textbf{Pairwise vote extraction and thresholding}\\[3pt]
    $\rightarrow$
    \textbf{Preference graph under regime $r$}
    $\rightarrow$
    \textbf{Mutual-pair, SCC, and FAS diagnostics}
    $\rightarrow$
    \textbf{Optional greedy repair}\\[3pt]
    $\rightarrow$
    \textbf{Ranking extraction and hybrid fusion}
    $\rightarrow$
    \textbf{nDCG evaluation and paired statistical analysis}\\[4pt]
    \emph{Raw-margin ablation: identical downstream pipeline, but without
    cross-ranker calibration and with fixed thresholds.}}}
  \caption{Preference-graph data-quality evaluation pipeline: upstream ranker
    scores $\to$ per-query/per-ranker calibration $\to$ pairwise vote
    extraction under a regime $r$ $\to$ preference graph $G_q$ $\to$
    structural diagnostics $\to$ optional repair producing
    $\widetilde G_q$ $\to$ ranking extraction on either $G_q$ or
    $\widetilde G_q$ $\to$ nDCG evaluation. The raw-margin ablation uses the
    same downstream pipeline but omits cross-ranker calibration and keeps the
    historical fixed thresholds.}
  \Description{A boxed left-to-right pipeline schematic showing stored ranker
    scores, per-query per-ranker calibration, pairwise vote extraction and
    thresholding, preference-graph construction under a vote regime, mutual-pair
    and SCC diagnostics, optional greedy repair, ranking extraction and hybrid
    fusion, and final nDCG evaluation with paired statistical analysis. A note
    states that the raw-margin ablation uses the same downstream pipeline
    without cross-ranker calibration and with fixed thresholds.}
  \label{fig:pipeline}
\end{figure}

\subsection{Upstream Scores and Candidate Pool}

The canonical pipeline starts from stored score files for three upstream
rankers: BM25, TF-IDF, and MiniLM. For each query $q$, we first define a
shared candidate pool $D_q$ from the union of documents scored by at least one
ranker. If that union already has size at most the dataset-specific pool size,
we keep all documents. Otherwise we rank the union by reciprocal rank fusion
with parameter $k=60$ over the full per-ranker native ranked lists and keep
the top-$|D_q|$ candidates. Candidate pooling therefore uses an RRF-family
computation, but it is distinct from both the Prior ranking and the reported
RRF baseline because it is applied before graph construction and before
candidate restriction.

\subsection{Per-Query, Per-Ranker Score Calibration}

The primary protocol does not treat native BM25, TF-IDF, and MiniLM margins as
directly comparable edge-weight contributions. For each query $q$ and ranker
$s$, let $D_{q,s} \subseteq D_q$ be the candidate documents for which ranker
$s$ supplies a score. We define the calibrated score
\begin{equation}
  \bar{x}_{q,s}(d) =
  \begin{cases}
    0, & \max_{d' \in D_{q,s}} x_{q,s}(d') - \min_{d' \in D_{q,s}} x_{q,s}(d') \le 10^{-12}, \\[4pt]
    \dfrac{x_{q,s}(d) - \min_{d' \in D_{q,s}} x_{q,s}(d')}
    {\max_{d' \in D_{q,s}} x_{q,s}(d') - \min_{d' \in D_{q,s}} x_{q,s}(d')},
    & \text{otherwise.}
  \end{cases}
  \label{eq:minmax-calibration}
\end{equation}
This transformation is applied separately for each query and ranker after
candidate restriction. Missing documents remain missing: if a ranker did not
score document $d$, no calibrated score is imputed for that document. If a
ranker's candidate-restricted score vector is constant, every calibrated score
is set to $0$ and that ranker contributes no nonzero within-query margin.

Calibration is necessary because the edge weights in a multi-ranker preference
graph inherit whatever comparability assumptions are imposed on the upstream
scores. We therefore treat the historical raw-margin construction only as an
ablation and use Eq.~\eqref{eq:minmax-calibration} as the canonical
graph-construction input.

\subsection{Pairwise Vote Extraction and Graph Construction}
\label{sec:vote-extraction}

For each query $q$, ranker $s$, and unordered candidate pair $\{u,v\}
\subseteq D_q$, the primary protocol compares calibrated scores only if both
documents are present in $D_{q,s}$. If either document is missing, ranker $s$
abstains on that pair. If $\bar{x}_{q,s}(u) = \bar{x}_{q,s}(v)$, ranker $s$
also abstains. Otherwise the absolute pairwise margin is
\begin{equation}
  m_{q,s}(u,v) = \left| \bar{x}_{q,s}(u) - \bar{x}_{q,s}(v) \right|.
  \label{eq:pair-margin}
\end{equation}
Ranker $s$ casts a directional vote only when $m_{q,s}(u,v) \ge \lambda_s$,
where $\lambda_s$ is the per-ranker vote-retention threshold for the current
dataset, protocol, and regime. The direction is given by the larger calibrated
score. For a candidate direction $u \rightarrow v$, let
\begin{equation}
  c_q(u,v) = \sum_{s} \mathbf{1}\!\left[s \text{ retains } u \rightarrow v\right]
  \label{eq:support-count}
\end{equation}
be the support count and
\begin{equation}
  a_q(u,v) = \sum_{s} m_{q,s}(u,v)\,
  \mathbf{1}\!\left[s \text{ retains } u \rightarrow v\right]
  \label{eq:aggregate-weight}
\end{equation}
be the aggregate directional weight. A direction is retained only if
\begin{equation}
  \begin{aligned}
    c_q(u,v) &\ge \kappa_r, \\
    a_q(u,v) &\ge \gamma_r,
  \end{aligned}
  \label{eq:retention-rule}
\end{equation}
where $\kappa_r$ is the regime-specific minimum support and $\gamma_r$ is the
aggregate threshold for the current dataset/protocol/regime cell. The graph
builder then sums all retained per-ranker contributions for the same directed
pair, so the final edge weight is $w_q(u,v)=a_q(u,v)$ whenever
Eq.~\eqref{eq:retention-rule} is satisfied. Because each direction is tested
independently, a single unordered pair can retain both $u \rightarrow v$ and
$v \rightarrow u$.

\subsection{Vote-Extraction Regimes}
\label{sec:regimes}

We evaluate three vote-extraction regimes over the same stored score files and
candidate pools:

\begin{itemize}
  \item \textbf{\texttt{ms2}} requires support $\kappa_r=2$ and applies a
    nontrivial aggregate threshold $\gamma_r$. Under the historical raw
    ablation this threshold is $0.1$; under the primary protocol it is
    retention-matched separately for each dataset.
  \item \textbf{\texttt{ms1}} requires support $\kappa_r=1$ and uses the
    dataset-specific aggregate threshold for that protocol/regime cell. It
    preserves directional disagreement whenever both directions independently
    survive thresholding.
  \item \textbf{\texttt{ms1\_drop\_mutual}} uses the same support rule as
    \texttt{ms1}, but after thresholding deletes every unordered pair that
    retained both directions. It therefore treats direct bidirectional
    contradiction as unresolved rather than retaining either arc.
\end{itemize}

The interpretation of these regimes is important. Cyclicity under
\texttt{ms1} does not automatically imply longer-cycle nontransitivity; it can
arise from direct mutual contradictions alone. We therefore separate mutual
pair diagnostics from residual cyclic structure after mutual-pair deletion.

\subsection{Retention-Matched Thresholds}

Under the primary protocol, thresholds are not fixed globally at the raw
values $0.05$ and $0.1$. Instead, for each dataset, regime, and calibration
variant, we match retention behavior to the historical raw construction
without using qrels. Let $\rho^{\mathrm{raw}}_{s,r}$ denote the raw retained
vote rate for ranker $s$ in regime $r$, computed over the stored query subset
with denominator equal to the number of candidate pairs for which both
documents are scored by $s$ and the native scores are unequal. For a
calibration variant, we pool all calibrated margins
$m_{q,s}(u,v)$ across the dataset and set
\begin{equation}
  \lambda_s = Q_{1-\rho^{\mathrm{raw}}_{s,r}}
  \left(\left\{m_{q,s}(u,v)\right\}\right),
  \label{eq:vote-threshold-match}
\end{equation}
where $Q_p$ denotes the empirical quantile at level $p$. If a ranker has no
eligible margins in that cell, we set $\lambda_s=0$.

After vote-threshold matching, we choose the aggregate threshold $\gamma_r$
deterministically to minimize the absolute gap between the retained edge count
under the calibrated construction and the retained edge count under the raw
reference construction for the same dataset and regime. The search is over the
realized aggregate candidate weights in that cell; when multiple thresholds
yield the same minimum gap, the smaller threshold is selected. Qrels are not
used at any stage of this threshold selection procedure. This matching
strategy keeps graph density and vote-retention behavior approximately
comparable across calibration variants without tuning for nDCG.

\subsection{Mutual Pairs and Nontrivial Cycles}

We distinguish two kinds of cyclic structure. A \emph{mutual pair} is a direct
contradiction in which both $u \rightarrow v$ and $v \rightarrow u$ are
retained. A \emph{nontrivial cycle} is any directed cycle that remains after
deleting all mutual-pair edges. The canonical results therefore report cyclic
query percentage and largest strongly connected component (SCC) size both
before and after mutual-pair deletion.

\subsection{Structural Diagnostics}
\label{sec:structural-metrics}

We group structural measurements into intrinsic graph diagnostics and
qrels-anchored diagnostics.

\textbf{Intrinsic graph diagnostics.} For each query graph we record edge
count, graph density $|E_q|/(|D_q|(|D_q|-1))$, mutual-pair count, the share of
queries containing any mutual pair, cyclic-query indicators before and after
mutual-pair deletion, largest SCC size before and after mutual-pair deletion,
directed triangle counts, total graph weight
$W_q=\sum_{(u,v)\in E_q} w_q(u,v)$, and normalized graph weight
$W_q/\max(1,|E_q|)$. After repair we also record the number of removed edges,
their total removed weight, and normalized removed weight
$W_q^{\mathrm{rem}}/W_q$ when $W_q>0$ and $0$ otherwise.

\textbf{Qrels-anchored diagnostics.} We additionally report two diagnostics
that compare a graph to an external reference ranking $\pi_{\mathrm{ref}}$
derived from relevance judgments: the \textbf{backward-edge weight}
\begin{equation}
  \mathrm{BEW}(\pi_{\mathrm{ref}}, G) \;=\!\! \sum_{(u,v)\in E:\ \pi_{\mathrm{ref}}(v) < \pi_{\mathrm{ref}}(u)} \!\! w(u,v),
  \label{eq:bew}
\end{equation}
and the \textbf{pairwise inconsistency count}
\begin{equation}
  \mathrm{PIC}(\pi_{\mathrm{ref}}, G) \;=\; \bigl|\{(u,v)\in E : \pi_{\mathrm{ref}}(v) < \pi_{\mathrm{ref}}(u)\}\bigr|,
  \label{eq:pic}
\end{equation}
the unweighted count of the same contradictions. These are not intrinsic graph
properties and are not independent validation signals: they use the same qrels
family as downstream retrieval evaluation. We therefore interpret them as
reference-alignment diagnostics rather than as separate evidence of retrieval
quality.

\subsection{Graph Repair}
\label{sec:repair}

The weighted feedback arc set optimization problem for graph $G_q$ is
\begin{equation}
  F_{\mathrm{opt}} \;=\; \operatorname*{arg\,min}_{F \subseteq E_q \,:\ (V_q,\, E_q\setminus F)\ \text{acyclic}} \ \sum_{(u,v)\in F} w_q(u,v),
  \label{eq:mwfas}
\end{equation}
with repaired graph $\widetilde G_q = (V_q, E_q \setminus F, w_q)$ when a set
$F$ is removed. In the canonical experiments we do \emph{not} solve
Eq.~\eqref{eq:mwfas} exactly. Instead, we compute a greedy removal set
$F_{\mathrm{greedy}}$ by repeatedly finding a directed cycle in the current
graph, removing the minimum-weight edge from that discovered cycle, and
continuing until no cycle remains. The implementation uses
\texttt{networkx.find\_cycle} to identify a cycle at each step.

This procedure approximately minimizes removed edge weight through greedy cycle
peeling, but it has no approximation guarantee and its output can depend on
cycle-discovery order. We therefore report removed weight and removed-edge
overlap empirically, without labeling the computed solution as an exact
minimum-weight feedback arc set. Repair remains a graph-internal operation: it
depends only on $w_q$, not on qrels or nDCG.

\subsection{Ranking Extraction}
\label{sec:extraction}

Ranking extraction is applied separately to $G_q$ and $\widetilde G_q$. Graph
construction and hybrid normalization must be kept distinct: the calibration
in Eq.~\eqref{eq:minmax-calibration} is applied before graph construction,
whereas the normalization in Eq.~\eqref{eq:hybrid} is applied only when
combining graph-derived scores with a prior ranking.

We evaluate three graph-derived ranking families. The \textbf{Copeland score}
is out-degree minus in-degree,
\begin{equation}
  s_{\mathrm{cop}}(u) \;=\; \deg^+(u) - \deg^-(u),
  \label{eq:copeland}
\end{equation}
counting net wins irrespective of edge weight. The \textbf{weighted balance
score} is the signed sum of incident edge weight,
\begin{equation}
  s_{\mathrm{bal}}(u) \;=\; \sum_{(u,v)\in E} w(u,v) \;-\; \sum_{(v,u)\in E} w(v,u).
  \label{eq:balance}
\end{equation}
The \textbf{Markov score} $s_{\mathrm{mc}}$ is the stationary distribution of
a Rank-Centrality-style Markov chain with damping $0.15$ on the weighted
preference graph~\cite{negahban2017rankcentrality}. Final rankings sort by
descending score with deterministic tie-breaking by document id when scores
are equal; the Markov ranking additionally breaks ties by smaller weighted
in-degree before document id.

The Prior ranking is a candidate-restricted RRF score over the stored ranker
lists with parameter $k=60$. The reported RRF baseline also uses $k=60$ on the
candidate-restricted lists, but its tie-breaking is not identical to Prior.
We therefore name those roles separately rather than treating Prior and RRF as
the same object.

The primary hybrid methods combine Prior with either Copeland or balance.
Writing $\hat{s}(\cdot)$ for min--max normalization of a score vector to
$[0,1]$ over the candidate set, the hybrid score is
\begin{equation}
  s_{\mathrm{hybrid}}(u) \;=\; \hat{s}_{\mathrm{prior}}(u) \;+\; \alpha\, \hat{s}_{\mathrm{comp}}(u),
  \qquad \mathrm{comp} \in \{\mathrm{cop}, \mathrm{bal}\},
  \label{eq:hybrid}
\end{equation}
with $\alpha=0.3$ in the main comparisons. The canonical package additionally
reports a small alpha sensitivity sweep over $\alpha \in \{0.1,0.3,0.5,1.0\}$
for this primary protocol only.

\subsection{Retrieval Evaluation}
\label{sec:retrieval-metrics}

Retrieval quality is measured by nDCG at the dataset-specific cutoff used in
candidate pooling. For a ranking $\pi$ and a relevance map $\mathrm{rel}:D_q
\to \mathbb{Z}_{\ge 0}$,
\begin{equation}
  \mathrm{nDCG@}k(\pi) \;=\; \frac{\displaystyle\sum_{i=1}^{k} \frac{2^{\mathrm{rel}(\pi_i)}-1}{\log_2(i+1)}}
  {\displaystyle\sum_{i=1}^{k} \frac{2^{\mathrm{rel}(\pi_i^{\mathrm{ideal}})}-1}{\log_2(i+1)}},
  \label{eq:ndcg}
\end{equation}
where $\pi_i$ is the document at rank $i$ in $\pi$ and $\pi^{\mathrm{ideal}}$
sorts candidate documents by descending relevance. nDCG@$k$ is defined as $0$
when the denominator is $0$. All paired comparisons use only the intersection
of queries for which both methods produce valid outputs.

\subsection{Raw-Margin Ablation}

The historical raw-margin protocol is retained only as an explicitly labeled
ablation. It uses native BM25, TF-IDF, and MiniLM scores without cross-ranker
calibration; a fixed per-vote threshold of $0.05$ for all three rankers; and
the original aggregate/support settings: \texttt{ms2} uses minimum support $2$
and aggregate threshold $0.1$, while \texttt{ms1} and
\texttt{ms1\_drop\_mutual} use minimum support $1$ and aggregate threshold
$0.0$. We do not interpret those raw margins as comparable confidence weights.

% =============================================================================
\section{Experimental Setup}
\label{sec:experimental-setup}

\subsection{Datasets and Stored Query Sets}
\label{sec:datasets}

We use four public retrieval benchmarks spanning distinct domains: SciDocs
(scientific document retrieval)~\cite{cohan2020scidocs}, FiQA (financial
opinion and question answering)~\cite{maia2018fiqa}, HotpotQA (multi-hop
question answering)~\cite{yang2018hotpotqa}, and BRIGHT (reasoning-intensive
retrieval)~\cite{su2024bright}. SciDocs, FiQA, and HotpotQA are also part of
BEIR~\cite{thakur2021beir}. The calibrated rerun does not draw a fresh query
sample. It reuses the fixed stored \texttt{query\_ids.txt} files from the
canonical experiment inputs and then filters them only for evaluation
eligibility.

A query is eligible if its qrels support ranking comparison: either at least
two judged documents with at least two distinct relevance grades, or at least
one judged document with strictly positive relevance. Queries failing this
criterion are excluded before downstream calibration and are recorded in a
query-exclusion audit. Table~\ref{tab:dataset-stats} reports the stored query
subset size, the usable query count under the calibrated package, the upstream
retrieval depth used for stored score generation, the candidate-pool size, and
the nDCG cutoff.

\begin{table}[t]
  \caption{Stored query subsets and evaluation settings reused by the
    calibrated four-dataset package.}
  \label{tab:dataset-stats}
  \begin{tabular}{lccccc}
    \toprule
    Dataset & Stored IDs & Usable queries & Top-$n$ & $|D_q|$ & nDCG@$k$ \\
    \midrule
    SciDocs  & 120 & 120 & 50 & 20 & 20 \\
    FiQA     & 120 & 120 & 50 & 20 & 20 \\
    HotpotQA & 70  & 52  & 35 & 10 & 10 \\
    BRIGHT   & 50  & 50  & 50 & 20 & 20 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Upstream Rankers and Candidate Pooling}
\label{sec:rankers}

The upstream score files are fixed across all protocols and regimes. They come
from BM25, TF-IDF, and MiniLM and are never regenerated in the calibrated
study. Candidate pooling uses the union of documents appearing in those score
files for a query; if the union exceeds the dataset-specific pool size, it is
truncated by RRF with $k=60$ over the full native ranked lists. The calibrated
study therefore evaluates a specific \emph{RRF-centered candidate-pool
setting}. It does not claim candidate-pool neutrality across alternative pool
construction policies.

The study uses exactly the same stored score files, candidate pools, and qrels
for all protocols. Differences among \texttt{ms2}, \texttt{ms1}, and
\texttt{ms1\_drop\_mutual} therefore reflect only vote extraction and graph
construction, not changes in upstream evidence.

\subsection{Methods and Baselines}
\label{sec:baselines}

The calibrated package evaluates four graph-independent baselines and ten
graph-dependent methods. The graph-independent baselines are Prior, RRF,
CombSUM, and Borda fusion. The graph-dependent methods are unrepaired and
repaired variants of Copeland, balance, and Markov extraction, plus unrepaired
and repaired Copeland and balance hybrids at $\alpha=0.3$.

CombSUM uses per-query, per-ranker min--max normalization before summation, so
missing documents contribute $0$ for that ranker. Borda fusion operates on the
rank positions of the stored score lists over the union of scored documents for
that query. Prior and the RRF baseline both use the RRF family with $k=60$ on
candidate-restricted lists, but they are not bitwise-identical implementations:
Prior ranks by fused prior score with document-id tie-breaking, whereas the RRF
baseline breaks ties by best observed rank and then document id. We therefore
report both while flagging that distinction explicitly.

\begin{table}[t]
  \caption{Method families evaluated in the calibrated four-dataset package.}
  \label{tab:baselines}
  \begin{tabular}{lcc}
    \toprule
    Method family & Uses graph? & Repaired and unrepaired? \\
    \midrule
    Prior & No & --- \\
    Reciprocal rank fusion (RRF) & No & --- \\
    CombSUM & No & --- \\
    Borda-count fusion & No & --- \\
    Copeland graph & Yes & Yes \\
    Balance graph & Yes & Yes \\
    Markov graph & Yes & Yes \\
    Copeland hybrid (Eq.~\eqref{eq:hybrid}) & Yes & Yes \\
    Balance hybrid (Eq.~\eqref{eq:hybrid}) & Yes & Yes \\
    \bottomrule
  \end{tabular}
\end{table}

The graph-independent baselines are identical across vote regimes for a fixed
query because they operate directly on the stored score files rather than on
the constructed preference graph. Regime-labeled baseline rows are reported
only to align comparisons with graph-dependent methods; they are not
independent reruns.

\subsection{Repair Configuration}
\label{sec:repair-variants}

All canonical results in the calibrated package use the greedy cycle-peeling
repair described in Section~\ref{sec:repair}. Table~\ref{tab:repair-variants}
records that primary configuration and clarifies the status of stronger-repair
checks. The latter are not part of the calibrated canonical evidence package
and are treated only as supplementary historical context.

\begin{table}[t]
  \caption{Repair configurations referenced in this manuscript revision.}
  \label{tab:repair-variants}
  \begin{tabular}{p{4.3cm}p{2.9cm}}
    \toprule
    Configuration & Role in this paper version \\
    \midrule
    Greedy cycle peeling & Primary repair for canonical and raw-ablation runs \\
    Stronger-repair checks from earlier audits & Supplementary only; not part of the calibrated canonical evidence package \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Statistical Analysis}
\label{sec:bootstrap}

All repaired-versus-unrepaired comparisons are paired at the query level. For
each cell we report the mean delta, median delta, 5th/25th/75th/95th-percentile
query-level deltas, and helped/harmed/unchanged query counts. We also report a
paired sign-flip permutation test and a paired percentile bootstrap interval
with 10{,}000 resamples. If $\delta_i$ denotes the per-query repaired minus
unrepaired nDCG difference for a paired set of $n$ queries, and
$\bar{\delta}^{(b)}$ is the mean of bootstrap resample $b$, the reported
percentile interval is
\begin{equation}
  \left[\, \bar\delta^{(\lceil 0.025(B-1)\rceil)},\; \bar\delta^{(\lceil 0.975(B-1)\rceil)} \,\right],
  \label{eq:bootstrap-ci}
\end{equation}
with $B=10{,}000$. Bootstrap summaries additionally include the fraction of
bootstrap means greater than zero.

The primary inferential family is the set of repaired-versus-unrepaired cells
under the canonical min--max plus retention-matched protocol. We apply both
Holm and Benjamini--Hochberg correction across that family. We do not treat a
bootstrap interval endpoint equal to zero as evidence of a strictly positive
effect, and we do not infer robustness from percentile intervals alone. For
interpretation-sensitive cells we additionally report leave-one-query-out and
top-influence-removal analyses.

\subsection{Implementation and Reproducibility}
\label{sec:implementation}

The calibrated core package is implemented in Python 3.12.3 using
\texttt{networkx} for graph operations. The package manifest records the exact
branch, commit, Python executable, activation command, stored score-file
hashes, qrels hashes, query-ID lists, protocol definitions, threshold tables,
and generated paper-package outputs. The canonical evidence package used for
this revision is the calibrated four-dataset artifact rooted at the internal
paper-package manifest produced by the full calibrated core run. Because this
is an anonymous review draft, we point to the artifact package descriptively
rather than through a public URL.

The calibrated rerun is deterministic given the stored inputs and recorded
seeds. In particular, it reuses fixed query-ID manifests instead of redrawing
query samples, and it uses fixed seeds for bootstrap and permutation analysis.
The study does not claim exact reproducibility for separate real-LLM judgment
pipelines.

\subsection{Real-LLM Evidence: Scope and Role}
\label{sec:real-llm-scope}

In addition to the mechanical three-ranker evaluation described above, we
inspect stored real-LLM judgment corpora on subsets of SciDocs, HotpotQA, and
FiQA, but we no longer treat those records as a parallel retrieval evaluation.
Section~\ref{sec:real-llm} reframes them as a bounded protocol-quality and
artifact audit because the stored corpora differ from the calibrated mechanical
study in prompt symmetry, parser behavior, exclusion logging, and provider
coverage. We flag that distinction here because those corpora are easy to
conflate with the main mechanical-vote evaluation if their weaker evidentiary
status is not made explicit.

% =============================================================================
\section{Structural Data Quality Results}
\label{sec:structural-results}

This section reports the calibrated canonical protocol introduced in
Section~\ref{sec:score-calibration}: per-query, per-ranker min--max score
normalization followed by retention-matched vote thresholds. The historical
raw-margin construction is retained only as an ablation for sensitivity
analysis. We separate four questions that were conflated in the earlier
raw-margin narrative: score-scale imbalance, graph construction, the share of
cyclicity attributable to direct mutual contradictions, and the amount of
structural work repair actually performs.

\subsection{Raw Score Scale Imbalance}

The raw protocol is strongly dominated by native BM25 scale. Under the
historical raw-margin construction, BM25 contributes a mean conditional edge
weight share of approximately $0.988$ whenever it participates in an edge; under
the calibrated primary protocol, the corresponding mean falls to approximately
$0.512$. This shift does not show that BM25 becomes less useful after
calibration. It shows that the old weighted graph was largely tracking one
ranker's score scale, not combining commensurate evidence from BM25, TF--IDF,
and MiniLM. The threshold-scale audit further shows that the fixed raw
thresholds were not semantically comparable across rankers, so raw score
margins cannot be interpreted as cross-ranker confidence weights.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/bm25_weight_share_raw_vs_calibrated.png}
  \caption{Mean conditional BM25 edge-weight share under the raw-margin
    ablation and the canonical calibrated protocol. The canonical protocol is
    per-query/per-ranker min--max calibration with retention-matched
    thresholds; the raw protocol is shown only as an ablation. The figure
    reports graph-construction weights rather than retrieval quality.}
  \Description{A grouped comparison of BM25 conditional weight share under the
    raw-margin ablation and the calibrated canonical protocol, showing a much
    larger BM25 contribution under the raw protocol and a near-balanced share
    after calibration.}
  \label{fig:bm25-share}
\end{figure}

\subsection{Calibration Changes Graph Construction}

\begin{table}[t]
  \caption{Intrinsic graph diagnostics under the canonical calibrated protocol.
    Cyclic\% is the share of query graphs containing at least one directed
    cycle before mutual-pair deletion; Post-mutual Cyclic\% recomputes the same
    statistic after deleting direct mutual pairs; Largest SCC is the mean size
    of the largest strongly connected component before deletion; normalized FAS
    is mean removed repair weight divided by total graph weight. Raw-margin
    results are treated only as an ablation and are not shown as the main
    evidence here.}
  \label{tab:structural-results}
  \scriptsize
  \begin{tabular}{llrrrr}
    \toprule
    Dataset & Regime & Cyclic\% & Post-mutual Cyclic\% & Largest SCC & Norm. FAS \\
    \midrule
    SciDocs  & \texttt{ms2}               & 0.0  & 0.0  & 1.00  & 0.000 \\
    SciDocs  & \texttt{ms1}               & 99.2 & 10.8 & 16.17 & 0.074 \\
    SciDocs  & \texttt{ms1\_drop\_mutual} & 11.7 & 11.7 & 1.93  & 0.002 \\
    FiQA     & \texttt{ms2}               & 0.0  & 0.0  & 1.00  & 0.000 \\
    FiQA     & \texttt{ms1}               & 98.3 & 30.8 & 16.12 & 0.080 \\
    FiQA     & \texttt{ms1\_drop\_mutual} & 30.8 & 30.8 & 3.38  & 0.004 \\
    HotpotQA & \texttt{ms2}               & 0.0  & 0.0  & 1.00  & 0.000 \\
    HotpotQA & \texttt{ms1}               & 63.5 & 1.9  & 4.06  & 0.029 \\
    HotpotQA & \texttt{ms1\_drop\_mutual} & 1.9  & 1.9  & 1.13  & 0.000 \\
    BRIGHT   & \texttt{ms2}               & 0.0  & 0.0  & 1.00  & 0.000 \\
    BRIGHT   & \texttt{ms1}               & 92.0 & 22.0 & 10.82 & 0.055 \\
    BRIGHT   & \texttt{ms1\_drop\_mutual} & 22.0 & 22.0 & 3.08  & 0.003 \\
    \bottomrule
  \end{tabular}
\end{table}

Calibration changes more than the numeric scale of existing edges. It changes
which edges are retained, how much total weight they carry, and which edges are
ultimately selected for repair. The overlap between the primary protocol's
removed edges and the raw-margin ablation is only $0.090$ on SciDocs
\texttt{ms1}, $0.055$ on FiQA \texttt{ms1}, $0.328$ on HotpotQA \texttt{ms1},
and $0.110$ on BRIGHT \texttt{ms1}. These low overlaps show that calibration
changes repair decisions themselves, not merely the magnitudes assigned to an
otherwise stable set of edges. Under the primary protocol, \texttt{ms2} remains
acyclic on all four datasets, while \texttt{ms1} is still highly cyclic before
mutual-pair deletion. Figure~\ref{fig:cyclicity-scc} summarizes that canonical
regime-level pattern and Figure~\ref{fig:raw-calibrated-structure} contrasts it
with the raw-margin ablation.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/cyclicity_primary_by_dataset_regime.png}
  \caption{Canonical calibrated cyclicity by dataset and vote regime. All
    values use per-query/per-ranker min--max calibration with
    retention-matched thresholds. The plot reports cyclicity before
    mutual-pair deletion; post-deletion values are summarized in
    Table~\ref{tab:structural-results} and Figure~\ref{fig:cycle-decomposition}.}
  \Description{A multi-panel plot showing cyclic query percentage under the
    canonical calibrated protocol for SciDocs, FiQA, HotpotQA, and BRIGHT
    across the ms2, ms1, and ms1 drop mutual regimes. ms2 is zero throughout,
    ms1 is highest on every dataset, and ms1 drop mutual is markedly lower.}
  \label{fig:cyclicity-scc}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/raw_vs_calibrated_structural_results.png}
  \caption{Raw-margin ablation versus canonical calibrated structure. The raw
    protocol uses native scores with fixed thresholds and is shown only as an
    ablation; the canonical protocol uses min--max calibration with
    retention-matched thresholds. The comparison illustrates that calibration
    changes edge retention, graph weight, cyclicity, and repair burden.}
  \Description{A raw-versus-calibrated comparison plot for structural metrics,
    showing visible shifts in graph weight, cyclicity, and repair burden across
    datasets and regimes when the canonical calibrated protocol replaces the
    raw-margin ablation.}
  \label{fig:raw-calibrated-structure}
\end{figure}

\subsection{Mutual Pairs Versus Residual Nontrivial Cyclicity}

The calibrated graphs remain cyclic under \texttt{ms1}, but a substantial share
of that cyclicity comes from direct mutual contradictions rather than longer
preference loops. Averaged over all dataset$\times$regime cells in the primary
protocol, cyclic-query prevalence is approximately $0.3495$ before mutual-pair
deletion and $0.1100$ afterward. The contrast is especially sharp on HotpotQA:
its \texttt{ms1} cyclic-query rate drops from $63.5\%$ to $1.9\%$ after direct
mutual pairs are deleted. SciDocs, FiQA, and BRIGHT retain more residual
nontrivial cyclicity after deletion ($10.8\%$, $30.8\%$, and $22.0\%$,
respectively), but the residual graphs are structurally much smaller than the
full \texttt{ms1} graphs. Figure~\ref{fig:cycle-decomposition} shows this
before/after decomposition directly.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/cyclicity_before_after_mutual_deletion.png}
  \caption{Canonical calibrated cyclicity before and after deleting direct
    mutual pairs. The plot distinguishes direct bidirectional contradiction from
    residual longer-cycle structure; it should not be read as evidence that all
    \texttt{ms1} cyclicity reflects nontrivial global nontransitivity.}
  \Description{A before-and-after comparison of cyclic query percentages under
    the canonical calibrated protocol, showing that deleting direct mutual pairs
    sharply reduces cyclicity on all datasets and leaves only a smaller residue
    of longer-cycle structure.}
  \label{fig:cycle-decomposition}
\end{figure}

\subsection{Structural Repair Activity}

Repair is active almost entirely where cyclicity remains active. Under the
primary protocol, \texttt{ms2} removes zero weight on all four datasets, while
\texttt{ms1} removes nontrivial normalized weight on every dataset and
\texttt{ms1\_drop\_mutual} removes only a small residual amount. Figure
\ref{fig:bew-pic} reports normalized removed weight directly. As a secondary
diagnostic, Table~\ref{tab:bew-pic} reports qrels-anchored BEW/PIC reductions
under the calibrated \texttt{ms1} graphs. These quantities are computed against
the same relevance-derived reference used later for nDCG@$k$ evaluation, so
they should be read as supporting diagnostics rather than independent evidence
that structural repair must improve retrieval.

\begin{table}[t]
  \caption{Qrels-anchored structural diagnostics under the canonical calibrated
    protocol, \texttt{ms1} only. $\Delta$BEW and $\Delta$PIC are mean
    pre-repair minus post-repair changes computed against the same
    relevance-derived reference ranking used for downstream nDCG evaluation;
    mean removed weight is the intrinsic repair objective. These diagnostics are
    therefore descriptive and qrels-anchored rather than independent validation.}
  \label{tab:bew-pic}
  \begin{tabular}{lccc}
    \toprule
    Dataset & $\Delta$BEW & $\Delta$PIC & Weight removed \\
    \midrule
    SciDocs  & 4.250 & 19.49 & 8.934 \\
    FiQA     & 3.919 & 18.24 & 7.974 \\
    HotpotQA & 0.563 & 2.25  & 1.173 \\
    BRIGHT   & 2.108 & 9.36  & 4.370 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/normalized_fas_weight_removed.png}
  \caption{Mean normalized FAS weight removed under the canonical calibrated
    protocol. This is an intrinsic graph diagnostic: the reported quantity is
    repair weight removed divided by total graph weight, before any retrieval
    evaluation. \texttt{ms2} is inactive by construction; most repair activity
    is concentrated in \texttt{ms1}.}
  \Description{A plot of normalized FAS weight removed across datasets and
    regimes under the canonical calibrated protocol, showing zero removal in
    ms2, substantial removal in ms1, and only small residual removal in
    ms1 drop mutual.}
  \label{fig:bew-pic}
\end{figure}

% =============================================================================
\section{Downstream Quality Results: Repair vs.\ Retrieval}
\label{sec:downstream-results}

The calibrated structural results above do not answer the retrieval question by
themselves. This section therefore evaluates repaired versus unrepaired ranking
quality directly under the canonical calibrated protocol, then asks whether any
positive point estimates survive paired testing, multiplicity correction, and
influence analysis. We next compare those calibrated graph-based methods with
simple fusion baselines, examine raw-versus-calibrated sign instability, and
close with the limited alpha sweep and balance-degeneracy audit.

\subsection{Repaired vs.\ Unrepaired Retrieval Under the Canonical Protocol}

Table~\ref{tab:bootstrap-delta-ndcg} reports the active \texttt{ms1} cells for
the calibrated Copeland graph and Copeland hybrid. Figure
\ref{fig:bootstrap-delta-ndcg} plots the complete calibrated primary grid,
including the many zero-effect \texttt{ms2} and \texttt{ms1\_drop\_mutual}
comparisons. Positive point estimates exist in several cells, but none remains
robust after the full testing and sensitivity pipeline reported below.

\begin{table}[t]
  \caption{Canonical calibrated repaired-minus-unrepaired nDCG@$k$ for the
    active \texttt{ms1} Copeland cells. Intervals are paired percentile
    bootstrap intervals over common queries; $p_{\mathrm{perm}}$ is the paired
    permutation test p-value on the same query set. No multiplicity adjustment
    is applied inside this table; corrected results are discussed in the next
    subsection.}
  \label{tab:bootstrap-delta-ndcg}
  \scriptsize
  \begin{tabular}{llcccc}
    \toprule
    Dataset & Pair & Mean $\Delta$ & 95\% CI & $p_{\mathrm{perm}}$ & $+/-/=$ \\
    \midrule
    SciDocs  & Copeland graph  & $+0.011$ & $[0.002, 0.022]$  & 0.020 & 36/22/62 \\
    SciDocs  & Copeland hybrid & $+0.009$ & $[0.002, 0.017]$  & 0.012 & 30/10/80 \\
    FiQA     & Copeland graph  & $+0.004$ & $[-0.005, 0.011]$ & 0.441 & 21/10/89 \\
    FiQA     & Copeland hybrid & $-0.005$ & $[-0.013, 0.002]$ & 0.301 & 16/8/96 \\
    HotpotQA & Copeland graph  & $+0.016$ & $[-0.010, 0.044]$ & 0.314 & 5/3/44 \\
    HotpotQA & Copeland hybrid & $+0.012$ & $[0.000, 0.031]$  & 0.253 & 3/0/49 \\
    BRIGHT   & Copeland graph  & $-0.014$ & $[-0.040, 0.006]$ & 0.267 & 19/13/18 \\
    BRIGHT   & Copeland hybrid & $+0.001$ & $[-0.016, 0.020]$ & 0.910 & 10/10/30 \\
    \bottomrule
  \end{tabular}
\end{table}

Most calibrated repaired-versus-unrepaired cells are exactly zero because
\texttt{ms2} is acyclic and \texttt{ms1\_drop\_mutual} is nearly acyclic on
most queries. The substantive activity is concentrated in \texttt{ms1}, but
even there the signs are mixed across datasets and methods. SciDocs
\texttt{ms1} is the strongest positive point-estimate case for Copeland, with
paired mean deltas of $+0.011$ for the graph ranking and $+0.009$ for the
hybrid. FiQA \texttt{ms1} shows a positive Copeland graph estimate but a
negative Copeland hybrid estimate. BRIGHT \texttt{ms1} is near zero for the
Copeland hybrid and negative for the Copeland graph. HotpotQA \texttt{ms1}
retains positive Copeland point estimates, but the effect is influence-sensitive
and does not survive the robustness checks below. The HotpotQA Markov graph
also changes sign relative to the raw-margin ablation, moving from a positive
mean in the raw protocol to a negative mean ($-0.0023$) under the calibrated
canonical protocol.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/repair_deltas_primary_with_intervals.png}
  \caption{Canonical calibrated repaired-minus-unrepaired deltas with paired
    95\% bootstrap intervals. Each point uses common-query pairing within a
    dataset, regime, and method pair. The figure is descriptive: multiplicity
    correction is applied later, and no positive cell remains robust after that
    correction.}
  \Description{A calibrated forest plot of repaired-minus-unrepaired nDCG
    deltas across datasets, regimes, and method pairs, with most cells at or
    near zero and only a few positive ms1 Copeland cells standing out before
    multiplicity correction.}
  \label{fig:bootstrap-delta-ndcg}
\end{figure}

\subsection{Multiplicity and Influence Robustness}

None of the positive repaired-versus-unrepaired cells remains robust after the
primary multiplicity correction family is applied. The two smallest raw paired
permutation p-values occur on SciDocs \texttt{ms1} Copeland hybrid
($p=0.012$) and SciDocs \texttt{ms1} Copeland graph ($p=0.020$), but both lose
support after Holm and Benjamini--Hochberg adjustment. The HotpotQA
\texttt{ms1} Copeland hybrid, which previously supplied the paper's positive
headline, has a fresh paired permutation p-value of approximately $0.253$ under
the canonical calibrated protocol and an interval whose lower endpoint is
exactly zero rather than strictly positive.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/hotpotqa_query_level_influence.png}

  \vspace{0.5em}

  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/scidocs_query_level_influence.png}
  \caption{Influence sensitivity for the two main positive-point-estimate cases
    discussed in the text. Top: HotpotQA \texttt{ms1} Copeland hybrid under the
    canonical calibrated protocol. Bottom: SciDocs \texttt{ms1} Copeland hybrid
    under the same protocol. These plots are paired-query diagnostics; they are
    descriptive and no multiplicity correction is applied within the figure.}
  \Description{Two stacked influence plots. The top plot shows HotpotQA
    copeland-hybrid query-level repaired-minus-unrepaired deltas under the
    canonical calibrated protocol, with a few positive queries dominating the
    mean. The bottom plot shows the corresponding SciDocs plot, where influence
    is more diffuse and the mean remains positive under top-query removal.}
  \label{fig:influence-robustness}
\end{figure}

HotpotQA's positive mean is concentrated in a handful of queries. Removing the
top one influential query reduces the calibrated \texttt{ms1} Copeland-hybrid
mean delta from $+0.0123$ to $+0.0053$; removing the top two reduces it to
$+0.0009$; removing the top three or top four drives the mean to exactly zero.
SciDocs is less concentrated: the corresponding Copeland-hybrid mean remains
positive after removing the top one, two, three, and four influential queries
($+0.0055$, $+0.0036$, $+0.0048$, and $+0.0041$), but these nominal positives
still do not survive multiplicity correction. The calibrated evidence therefore
supports influence-sensitive positive point estimates in a small number of
cells, not a robust positive retrieval effect of repair.

\subsection{Raw Versus Calibrated Sign Instability}

Calibration changes retrieval conclusions as well as structure. Table
\ref{tab:raw-calibrated-ablation} lists the five most consequential sign flips
between the raw-margin ablation and the canonical calibrated protocol. These
flips are not incidental numeric perturbations: they accompany large shifts in
edge retention, edge weighting, and removed-edge overlap, so they show that the
paper's downstream conclusions are not calibration invariant.

\begin{table}[t]
  \caption{Sign changes between the raw-margin ablation and the canonical
    calibrated protocol. The raw protocol uses native heterogeneous scores with
    fixed thresholds and is shown only as an ablation. The calibrated protocol
    uses per-query/per-ranker min--max normalization with retention-matched
    thresholds. Values are repaired-minus-unrepaired mean nDCG@$k$.}
  \label{tab:raw-calibrated-ablation}
  \begin{tabular}{llcc}
    \toprule
    Dataset & Cell & Raw & Calibrated \\
    \midrule
    SciDocs  & \texttt{ms1} Copeland graph  & $-0.0079$ & $+0.0110$ \\
    SciDocs  & \texttt{ms1} Copeland hybrid & $-0.0018$ & $+0.0085$ \\
    FiQA     & \texttt{ms1} Copeland graph  & $-0.0029$ & $+0.0036$ \\
    BRIGHT   & \texttt{ms1} Copeland hybrid & $-0.0045$ & $+0.0006$ \\
    HotpotQA & \texttt{ms1} Markov graph    & $+0.0087$ & $-0.0023$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/raw_vs_calibrated_retrieval_deltas.png}
  \caption{Raw-margin ablation versus canonical calibrated repaired-minus-unrepaired
    retrieval deltas. The raw protocol is an ablation only; the calibrated
    protocol is the main evidence. Sign changes indicate that retrieval
    conclusions are not stable under score-scale handling.}
  \Description{A raw-versus-calibrated comparison plot of repaired-minus-unrepaired
    retrieval deltas, highlighting sign changes in several dataset and method
    cells when the canonical calibrated protocol replaces the raw-margin
    ablation.}
  \label{fig:raw-calibrated-deltas}
\end{figure}

\subsection{Comparison with Fixed Aggregation Baselines}

The calibrated repaired-versus-unrepaired analysis isolates the marginal effect
of repair, but it does not answer whether graph-based methods outperform simple
fusion baselines. Table~\ref{tab:pooled-baseline} therefore reports dataset
macro-averages under the primary \texttt{ms1} protocol, and
Figure~\ref{fig:mean-ndcg-hybrids} shows the corresponding per-dataset baseline
comparison. We use dataset-level summaries rather than pooled
query$\times$regime means because the calibrated conclusions are not uniform
across datasets.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/per_dataset_baseline_comparison.png}
  \caption{Per-dataset baseline comparison under the canonical calibrated
    protocol. Intervals, where shown, are descriptive bootstrap summaries
    within a dataset rather than multiplicity-adjusted paired tests across the
    whole figure. The comparison separates the strong simple baselines from the
    repaired-versus-unrepaired question.}
  \Description{A per-dataset comparison of ranking methods under the canonical
    calibrated protocol, showing simple baselines such as CombSUM, RRF, and the
    prior alongside graph-based and hybrid methods.}
  \label{fig:mean-ndcg-hybrids}
\end{figure}

\begin{table}[t]
  \caption{Dataset-macro comparison under the canonical calibrated protocol,
    \texttt{ms1} only. Mean nDCG is averaged across the four datasets; average
    rank is the mean within-dataset rank by mean nDCG. Because Prior and RRF
    use closely related but not identical implementations, both are reported
    separately rather than treated as the same baseline.}
  \label{tab:pooled-baseline}
  \begin{tabular}{lcc}
    \toprule
    Method & Macro mean nDCG & Avg. rank \\
    \midrule
    CombSUM                  & \textbf{0.554} & 3.25 \\
    Balance hybrid unrepaired& 0.548 & 4.75 \\
    Balance hybrid repaired  & 0.547 & 6.00 \\
    RRF                      & 0.546 & 5.50 \\
    Copeland hybrid repaired & 0.546 & 6.50 \\
    Prior                    & 0.545 & 6.75 \\
    Borda fusion             & 0.545 & 6.00 \\
    Balance repaired         & 0.539 & 8.25 \\
    Balance unrepaired       & 0.539 & 8.25 \\
    Markov unrepaired        & 0.537 & 9.50 \\
    Markov repaired          & 0.537 & 9.25 \\
    Copeland repaired        & 0.534 & 11.00 \\
    Copeland unrepaired      & 0.530 & 11.00 \\
    \bottomrule
  \end{tabular}
\end{table}

Simple fusion baselines remain strong after calibration. CombSUM has the
highest dataset-macro mean under primary \texttt{ms1}; RRF is also competitive,
and both outperform the repaired Copeland graph methods on this summary. The
Prior baseline uses the same RRF family on candidate nodes but differs from the
standalone RRF baseline in implementation details and exact tie-breaking, so we
report both rather than treating them as interchangeable; across the full run,
their exact rankings match in only 216 of 6,156 comparable cases. The
calibrated evidence therefore does not support a claim that graph repair makes
simple baselines obsolete.

\subsection{Alpha Sensitivity and Balance Degeneracy}

The small alpha sweep does not rescue a robust positive repair result.
Table~\ref{tab:alpha-sensitivity} reports the primary \texttt{ms1}
Copeland-hybrid delta over $\alpha\in\{0.1, 0.3, 0.5, 1.0\}$. SciDocs remains
positive for $\alpha\in\{0.3,0.5,1.0\}$, but those gains are still not robust
after multiplicity correction. HotpotQA stays positive for
$\alpha\in\{0.3,0.5,1.0\}$, but the effect remains influence-sensitive. FiQA
and BRIGHT change sign within this small sweep, showing that stronger graph
weight does not induce a stable benefit.

\begin{table}[t]
  \caption{Copeland-hybrid alpha sensitivity under the canonical calibrated
    protocol, \texttt{ms1} only. Entries are repaired-minus-unrepaired mean
    nDCG@$k$; this is a small sensitivity sweep, not a tuned hyperparameter
    search.}
  \label{tab:alpha-sensitivity}
  \begin{tabular}{lrrrr}
    \toprule
    Dataset & $\alpha=0.1$ & $\alpha=0.3$ & $\alpha=0.5$ & $\alpha=1.0$ \\
    \midrule
    SciDocs  & $-0.0022$ & $+0.0085$ & $+0.0097$ & $+0.0083$ \\
    FiQA     & $+0.0006$ & $-0.0046$ & $+0.0010$ & $+0.0038$ \\
    HotpotQA & $-0.0008$ & $+0.0123$ & $+0.0120$ & $+0.0101$ \\
    BRIGHT   & $-0.0013$ & $+0.0006$ & $-0.0074$ & $-0.0071$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{../../../reports/full_calibrated_core/outputs/calibrated_all4/paper_package/plots/alpha_sensitivity.png}
  \caption{Alpha sensitivity for the canonical calibrated hybrids. The plot is
    descriptive: it shows how repaired-minus-unrepaired means move as the graph
    component receives more or less weight, but it does not constitute a
    hyperparameter search or multiplicity-adjusted test.}
  \Description{A multi-line plot of repaired-minus-unrepaired mean nDCG by
    alpha under the canonical calibrated protocol for the four datasets,
    showing that signs and magnitudes remain small and unstable.}
  \label{fig:alpha-sensitivity}
\end{figure}

The balance degeneracy audit explains why many balance results remain exactly
or nearly zero despite nontrivial graph changes. Under the primary
\texttt{ms1} protocol, the graph changes on 119/120 SciDocs queries, 118/120
FiQA queries, 33/52 HotpotQA queries, and 46/50 BRIGHT queries. Those changes
do not consistently propagate to the evaluation metric: the full
balance-hybrid ranking changes on only 68, 68, 7, and 20 queries respectively,
and nDCG changes on only 8, 5, 1, and 8 queries. In other words, the graph can
change, the balance scores can change, and even the top-$k$ order can change
without altering the relevance sequence scored by nDCG@$k$. The balance results
are therefore not mysterious nulls; they are a direct consequence of
downstream propagation failure from graph repair to the evaluated prefix.

% =============================================================================
\clearpage
\section{Secondary Analyses and Scope Checks}
\label{sec:secondary-checks}

The calibrated core evidence of this paper is contained in
Sections~\ref{sec:structural-results} and~\ref{sec:downstream-results}. The
four topics below are retained only as scoped secondary notes because they do
not currently meet the same evidentiary standard as the calibrated mechanical
evaluation.

\subsection{Rule-Based Outcome Taxonomy}
\label{sec:failure-taxonomy}

An earlier draft summarized repair outcomes with a six-way failure taxonomy
computed over a separate 1,020-record raw-margin failure-mining corpus. The
blocking audit showed that this taxonomy is not manually verified: it is an
automated rule cascade whose final labels depend heavily on branch precedence,
and its counts were never regenerated for the calibrated canonical protocol.
Because calibration changes retained edges, removed edges, and
repaired-versus-unrepaired signs, those historical counts are not appropriate
as current evidence for the revised paper.

We therefore remove the old taxonomy table and figure from the main empirical
narrative. A calibrated \emph{rule-based outcome taxonomy} may still be useful
as future descriptive analysis, but it would need to be regenerated under the
canonical protocol before interpretation. Such a taxonomy would remain a
retrospective outcome classification, not a validated predictive mechanism
available before repair.

\subsection{Protocol Audit of Stored LLM Judgments}
\label{sec:real-llm}

We also inspected stored real-LLM judgment corpora to determine whether they
provide an independent validation of the calibrated mechanical study. They do
not currently support that role. The stored corpora exhibit parser defaults
that silently map ambiguous or malformed outputs to a fixed label, measurable
provider-dependent position bias, incomplete raw-text and exclusion logging,
and protocol differences across providers and method families. These issues are
serious enough that apparent cyclicity in a naively constructed LLM-derived
graph can be dominated by pipeline artifacts rather than by genuine model
disagreement.

For that reason, we do not pool the stored LLM corpora with the canonical
mechanical-vote evaluation or treat them as a parallel replication. The main
lesson from these records is methodological: pairwise-judgment pipelines can
manufacture apparent inconsistency if prompt symmetry, abstention behavior,
parser handling, and exclusion accounting are not controlled carefully.
Accordingly, we retain this material only as a bounded artifact audit.

A future LLM-based study intended to support the paper's central claims would
need randomized candidate order, symmetric A/B and B/A prompting, abstention on
malformed or ambiguous outputs, no default-label fallback, complete exclusion
logs, and matched query/method protocols across providers.

\subsection{Implementation Cost Note}
\label{sec:efficiency}

We retain only a bounded implementation-cost observation. The calibrated core
pipeline is CPU-only and practical on the graph sizes studied here, but the
available runtime and memory evidence comes from a single unbenchmarked local
machine with coarse resident-set-size snapshots and some timings near the
instrumentation floor. We therefore do not present efficiency as a substantive
contribution or make cross-system scaling claims. Repair cost depends on graph
size, density, and SCC structure, and a controlled hardware benchmark would be
required before stronger conclusions could be justified.

\subsection{Planned CARB Resource}
\label{sec:carb}

We do not present CARB as a released benchmark in this review draft. A
feature-only companion resource remains planned, but it is not part of the
current anonymous review package, no empirical claim in this paper depends on
its availability, and earlier draft release counts were not sufficiently
reconciled to retain as settled manuscript facts. Any future CARB release would
need a review-ready package, corrected scope statistics, and a licensing review
consistent with the source datasets' redistribution terms.

% =============================================================================
\section{Discussion}
\label{sec:discussion}

Sections~\ref{sec:structural-results}--\ref{sec:secondary-checks} support a
more specific conclusion than the manuscript's earlier raw-margin version:
preference-graph repair cannot be interpreted independently of the score
calibration and vote-construction protocol that creates the graph. Under the
canonical \texttt{primary\_minmax\_retention\_matched} protocol, repair still
changes graph structure, but its downstream retrieval effect remains
calibration-sensitive, method-dependent, and not robustly positive across the
tested benchmarks.

\textbf{Calibration is part of the model, not preprocessing trivia.} The
preference graph inherits whatever score scale is used to construct pairwise
margins. In the historical raw package, those margins were not comparable across
BM25, TF-IDF, and MiniLM, and BM25 contributed almost all conditional edge
weight when it participated. The calibrated primary protocol substantially
reduced that scale domination and, with it, changed which votes survived, which
edges were retained, and which edges were removed during repair. The practical
lesson is methodological: conclusions about graph repair are inseparable from
the calibration and threshold semantics used upstream. We do not claim that
per-query, per-ranker min--max normalization is universally optimal, only that
it is the best-supported canonical protocol in this repository because it
materially reduced score-scale distortion and enabled a controlled comparison
against the raw ablation.

\textbf{Structural success does not guarantee retrieval gain.} FAS repair
(Eq.~\eqref{eq:mwfas}) optimizes a graph-internal objective, whereas nDCG
evaluates ranked retrieval against qrels. That distinction matters empirically:
the calibrated experiments still show nonzero repair activity in the cyclic
regimes, yet structural improvement does not imply a better final ranking.
Depending on the query, extraction rule, and fusion stage, a repaired graph can
be retrieval-inactive, evaluation-invisible, neutral, or harmful. The
qrels-anchored diagnostics in Section~\ref{sec:structural-results} are useful
for interpretation, but they should not be treated as independent validation of
the graph objective itself. Structural consistency and retrieval effectiveness
remain related but distinct empirical targets.

\textbf{Mutual contradictions and longer-cycle structure should be reported
separately.} The calibrated results also change how cyclicity should be read.
Apparent graph cyclicity is substantial under the primary protocol before mutual
pair deletion, but much of it arises from direct bidirectional contradictions
rather than from longer cyclic structure. After deleting mutual pairs, the mean
cyclic-query rate drops markedly, leaving a smaller but still nonzero residual
of nontrivial cyclicity. This does not make the remaining cycles unimportant,
but it does mean that total \texttt{ms1} cyclicity should not be interpreted
automatically as genuine Condorcet-style nontransitivity. Future work should
report both direct mutual contradiction and post-deletion cyclicity by default.

\textbf{Repair effects are method- and calibration-dependent.} The same repaired
graph does not propagate uniformly through Copeland, Markov, balance, and the
hybrid variants. Some calibrated cells change sign relative to the raw package,
and the balance-family zero-delta pattern in this study reflects propagation
failure through the ranking pipeline rather than a theorem that balance-based
repair can never matter. The hybrid results similarly show that fusion can
either dampen noisy graph changes or suppress potentially meaningful ones,
depending on how much signal survives calibration and how strongly the prior
ranking dominates the final score. Positive point estimates therefore require
more than a favorable mean: in this study, none remains robust once paired
testing, influence analysis, and multiplicity correction are applied together.
HotpotQA is the clearest example. Its calibrated \texttt{ms1} Copeland-hybrid
mean is positive, but the effect is concentrated in a few influential queries,
the paired permutation test is not compelling, and the cell does not survive
Holm or Benjamini--Hochberg adjustment.

\textbf{Simple baselines remain important under calibrated graphs.} The revised
paper also does not support graph repair as a superior default retrieval
strategy. Reciprocal-rank fusion and CombSUM remain strong comparators under the
calibrated protocol, and the prior ranking continues to absorb much of the
useful signal in the hybrids. At the same time, these comparisons should be
read with the candidate-pool design in mind: the experiments are centered on an
RRF-family pooling scheme, and Prior and the RRF baseline, although related, are
not interchangeable rows. The right takeaway is therefore cautious rather than
triumphalist: simple fusion baselines remain strong under the calibrated
protocol, and graph-dependent methods must justify their added complexity on a
paired, dataset-specific basis.

\textbf{Implications for future preference-graph studies.} Preference-graph
experiments should report calibration, threshold interpretation, common-query
pairing, mutual-pair prevalence, residual nontrivial cyclicity, repair
objective, ranking-extraction method, influence diagnostics, and multiplicity
correction as first-class design choices rather than hidden preprocessing
details. Raw-score constructions can still be informative as ablations, but not
as canonical evidence when the underlying rankers operate on heterogeneous score
scales. This paper therefore does not offer a validated predictive selector for
when repair will help. It offers a narrower but more defensible conclusion:
repair outcomes depend materially on the graph-construction process, and any
future claim about repair effectiveness should make that dependence explicit.

% =============================================================================
\section{Limitations}
\label{sec:threats}

The revised study has a clearer limitations profile than the earlier raw-margin
version, but it still has important boundaries.

\textbf{Calibration remains a design choice, not a proven optimum.} The
canonical protocol uses per-query, per-ranker min--max normalization with
retention-matched thresholds because that combination substantially reduced
score-scale domination and supported the most defensible controlled comparison
in this repository. It is not the only plausible calibration. Other
transformations in the pilot changed graph structure and some downstream signs,
so the present paper demonstrates calibration sensitivity rather than proving
that one calibration is universally best.

\textbf{Candidate pooling and baseline roles are RRF-centered.} The experiments
use an RRF-family candidate-pool construction and then compare graph methods
against Prior, RRF, CombSUM, Borda, and related variants within that setting.
Because candidate selection, Prior, and the RRF baseline occupy different
implementation roles, conclusions may differ under independently constructed
pools or different prior rankings. The paper documents these distinctions, but
it does not establish pool-invariant behavior.

\textbf{Nontrivial cyclicity is smaller than headline cyclicity.} Under the
primary protocol, cyclicity after removing direct mutual pairs is materially
lower than cyclicity before deletion. That means some regimes offer limited
opportunity for repair to act on longer-cycle structure, and the paper does not
establish how the same methods would behave on corpora with naturally high
nontrivial cyclicity.

\textbf{The repair algorithm is heuristic and order-sensitive.} Our main repair
procedure is greedy cycle peeling with no approximation guarantee. The
exact-for-small-components variant improves coverage only for bounded
subproblems and is not a globally exact solver for the full graph family. In
principle, cycle-discovery order, insertion order, and alternative optimization
strategies could produce different repaired graphs and therefore different
downstream rankings.

\textbf{Statistical power and influence remain limiting factors.} Several cells
have modest query counts, and some positive-looking means are concentrated in a
small number of influential queries. The paper addresses this with paired
permutation tests, bootstrap intervals, leave-one-out analysis, influence
removal, and multiplicity correction, but those diagnostics also reinforce the
main caution: no positive repaired-versus-unrepaired effect remains robust after
Holm or Benjamini--Hochberg correction.

\textbf{Metric scope is narrower than graph scope.} nDCG is the main retrieval
metric, and its cutoff determines whether some ranking changes are visible at
all. The qrels themselves are incomplete and dataset-dependent, and BEW/PIC are
computed from the same relevance evidence rather than from an independent source
of graph truth. The paper therefore does not claim that improved graph
consistency implies a general improvement in user utility beyond the measured
retrieval setting.

\textbf{The stored LLM records are audit-limited.} As Section~\ref{sec:real-llm}
now makes explicit, parser defaults, position bias, incomplete logging, and
protocol differences prevent the current LLM artifacts from supporting a clean
claim about genuine model nontransitivity or from serving as independent
validation of the calibrated mechanical study.

\textbf{External validity remains bounded.} The canonical evidence is drawn from
four retrieval benchmarks, three mechanical rankers, and a specific set of vote
regimes, extraction methods, and fusion choices. We do not claim that the same
calibrated conclusions will hold unchanged for other domains, ranker families,
pairwise-preference sources, or evaluation metrics.

\textbf{Artifact status is still evolving.} CARB remains a planned resource
rather than a released benchmark, and no claim in this paper depends on its
availability. The current anonymous manuscript should therefore be evaluated on
the calibrated evidence package reported here, not on a promised external
benchmark release.

% =============================================================================
\section{Conclusion}
\label{sec:conclusion}

This paper revisited a narrower question than the manuscript's original title
implied: not simply whether preference-graph repair helps retrieval, but how
that answer depends on the graph-construction protocol used to define pairwise
evidence in the first place. The central correction is methodological. Raw
heterogeneous ranker margins severely distorted weighted graph construction,
producing near-total BM25 dominance whenever BM25 participated. Replacing those
raw margins with per-query, per-ranker min--max calibration and
retention-matched thresholds materially changed retained edges, cyclicity,
removed edges, and several repaired-versus-unrepaired signs.

Under this calibrated canonical protocol, repair still improves internal graph
consistency in the active regimes, but structural consistency and retrieval
effectiveness do not move together reliably. Positive repaired-versus-unrepaired
point estimates remain in some cells, yet none survives the combined scrutiny
of paired testing, influence analysis, and multiplicity correction. The paper
therefore does not support a calibration-invariant claim that repair helps, nor
does it support a single robust positive exception. It instead shows that
repair outcomes are calibration-sensitive and method-dependent, and that simple
fusion baselines remain strong comparators under the calibrated protocol.

The practical implication is modest but important: preference-graph repair
should be evaluated together with the score calibration, threshold semantics,
mutual-pair handling, and ranking-extraction choices that determine the graph on
which repair operates. Future work may yet find settings where repair yields a
robust retrieval gain, but this study shows that such a claim cannot be treated
as independent of graph construction.

% =============================================================================
\section{Data Availability and Reproducibility}
\label{sec:data-availability}

\textbf{Data availability.} All four source datasets (SciDocs, FiQA,
HotpotQA, BRIGHT) are publicly available from their original
sources~\cite{cohan2020scidocs,maia2018fiqa,yang2018hotpotqa,su2024bright}
under their respective licenses; this paper introduces no new raw-text
corpus, and no third-party raw document collection is redistributed by this
manuscript. CARB is a planned feature-only companion resource
(Section~\ref{sec:carb}), not a released benchmark, and no empirical claim in
this paper depends on its present availability.

\textbf{Artifact availability.} The full pipeline (vote extraction, graph
construction, repair, ranking extraction, evaluation, and statistical analysis;
Sections~\ref{sec:methodology}--\ref{sec:experimental-setup}) is implemented
in a single code repository. For double-blind review, the repository
identity and URL are withheld; an anonymized or author-provided artifact
will be made available to reviewers upon request through the venue's
standard mechanism, and a citable release may follow acceptance or the
camera-ready process. The artifact package contains the stored ranker scores
used by the mechanical experiments, query-ID manifests, qrels and score-file
hashes, calibrated and raw-ablation output packages, manuscript-ready tables
and figures, and run manifests that record the exact branch and commit used to
generate them. The canonical evidence package is the calibrated
\texttt{primary\_minmax\_retention\_matched} package; the raw-margin package is
retained only as a historical ablation.

\textbf{Reproducibility.} The calibrated mechanical results reported in
Sections~\ref{sec:structural-results}--\ref{sec:downstream-results} are
generated from stored intermediate artifacts and deterministic scripts. The
repository records the canonical calibrated package, the historical raw-margin
ablation package, threshold manifests, common-query manifests, and the tables
and figures included in the manuscript. These materials are sufficient to
regenerate the reported calibrated tables and plots from the stored mechanical
inputs without rerunning upstream retrieval models or external paid APIs. Exact
reproduction of future real-LLM outputs is not guaranteed because provider
models and serving behavior can change over time, and the stored LLM records in
this repository are retained only for bounded protocol audit purposes rather
than as canonical evidence. No public repository URL is included in this
anonymous manuscript; release details will be finalized after review.

\begin{acks}
\end{acks}

\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
