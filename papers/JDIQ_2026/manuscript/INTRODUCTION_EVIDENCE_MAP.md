# Introduction Evidence Map

**Prepared:** 2026-07-12
**Scope:** Every quantitative or empirical sentence in `main.tex` §1 Introduction, traced to a canonical source. No new experiments, no recalculation.

Sentence IDs (`I-1`, `I-2`, ...) follow paragraph order in the Introduction. Purely motivational, rhetorical, or citation-only sentences (no empirical/quantitative content) are omitted, per task instructions.

**Changelog (Sections 2-4 consistency pass):** two non-substantive wording fixes were made to the Introduction during the acronym-consistency audit for Sections 2-4: (1) "nDCG" was expanded to "normalized discounted cumulative gain (nDCG)" at its first use (paragraph 3), and (2) "(RRF)" was appended after "reciprocal rank fusion" at its first use (paragraph 6, sentence I-13) so the acronym is defined before Section 4's baseline table uses it bare. Neither changes the claim, evidence, or confidence of any row below; no row was added, removed, or reclassified as a result.

---

| ID | Sentence (paraphrased pointer) | Claim | Canonical source file | Table/row | Claim type | Confidence | Caveat |
|---|---|---|---|---|---|---|---|
| I-1 | "aggregating [preferences] into a graph... can contain directed cycles, meaning the aggregated evidence cannot be represented by any single, globally consistent ordering" | Preference graphs derived from multi-ranker votes can be cyclic | `outputs/pub_vote_cmp_all4/paper_package/tables/table_graph_ndcg_and_consistency.csv` | `pct_cyclic` column, all 12 rows | Descriptive | High | General structural fact, not regime-specific; regime-specific magnitudes given later (I-6) |
| I-2 | "Feedback-arc-set (FAS) repair... removes the minimum-weight set of edges needed to make the graph acyclic" | Definition of the repair operator | `src/consistency_ranker/` (repair implementation); described in `docs/LITERATURE_ALIGNMENT.md` §1.1 | N/A (method definition) | Descriptive | High | `docs/LITERATURE_ALIGNMENT.md` explicitly cautions this is a **greedy heuristic**, not an exact minimum-weight solver — the Introduction says "removes the minimum-weight set of edges" in a definitional/idealized sense; the precise greedy-vs-exact distinction is deferred to §3/§6 (out of scope for this task) and to `IJCS_REUSE_AUDIT.md` §3.3, which flags the same caveat |
| I-3 | "It is possible for a repair intervention to measurably improve graph-level consistency while leaving the final ranked list unchanged, or even making it worse" | Structural improvement can be decoupled from, or contrary to, retrieval improvement | `outputs/pub_vote_cmp_all4/paper_package/tables/table_bootstrap_delta_ndcg.csv` (SciDocs ms1 copeland: mean Δ = −0.000127, i.e. directionally negative) cross-referenced with `table_consistency_qrels_bew.csv` (SciDocs ms1: ΔBEW/ΔPIC both positive, i.e. structurally improved) | `table_bootstrap_delta_ndcg.csv` row `scidocs,ms1,copeland`; `table_consistency_qrels_bew.csv` row `scidocs` (ms1) | Interpretive (general possibility, illustrated by SciDocs) | Medium | SciDocs's negative point estimate has a CI that straddles zero (`ci_relation_to_zero = straddles_zero` per `papers/JDIQ_2026/figure4_evidence/figure4_bootstrap_data.csv`), so this sentence is worded as a possibility ("it is possible... or even making it worse"), not as a confirmed harm claim — deliberately more conservative than `docs/SAFE_CLAIMS_FOR_PAPER.md`'s SC-4/SC-5, which cite the deprecated `pub_vote_cmp_v2` package and claim a strictly negative CI; that claim is **not reused** here (see `IJCS_REUSE_AUDIT.md` headline finding) |
| I-4 | "four public retrieval benchmarks (SciDocs, FiQA, HotpotQA, and BRIGHT) and three vote-extraction regimes" | Study scope: 4 datasets × 3 regimes | `outputs/pub_vote_cmp_all4/paper_package/tables/table_graph_ndcg_and_consistency.csv` | All 12 rows (dataset × variant) | Descriptive | High | None |
| I-5 | "vote-extraction regime alone determines whether a graph has any cyclic structure for repair to act on" | Regime, not repair, is the primary driver of cyclicity | `table_graph_ndcg_and_consistency.csv` | `pct_cyclic`, `mean_fas_weight_removed` columns (ms2/ms1_drop_mutual ≈ 0% cyclic, ≈0 weight removed vs. ms1 51.9–95% cyclic) | Descriptive | High | BRIGHT `ms1_drop_mutual` is a partial exception (6% cyclic, 0.0088 mean weight removed, still non-zero) — a mild residual, noted but not contradicting the general claim |
| I-6 | "a conservative, majority-style regime keeps graphs almost entirely acyclic on every dataset we test, while a permissive regime... produces substantially cyclic graphs with much larger strongly connected components" | `ms2` ≈ 0% cyclic / SCC ≈ 1.0 vs. `ms1` 51.9–95.0% cyclic / SCC 2.5–12.5 across all four datasets | `table_graph_ndcg_and_consistency.csv` | `pct_cyclic`, `avg_largest_scc` columns, all rows | Descriptive | High | None |
| I-7 | "structural improvement from repair and downstream retrieval improvement are decoupled in the large majority of the conditions we test" | 20 of 24 canonical repaired-vs-unrepaired cells show exactly zero ΔnDCG | `outputs/pub_vote_cmp_all4/paper_package/tables/table_bootstrap_delta_ndcg.csv`; independently re-derived and quality-checked in `papers/JDIQ_2026/figure4_evidence/figure4_bootstrap_data.csv` | 20/24 rows with `ci_relation_to_zero = exactly_zero` | Inferential (aggregate count) | High | "Large majority" = 20/24 (83%); exact count traceable to the Figure 4 evidence CSV built in this same workspace |
| I-8 | "When the underlying graph is already near-acyclic, repair is retrieval-inactive by construction, because there is negligible conflicting edge weight for it to remove" | Mechanistic explanation for null rows | `table_graph_ndcg_and_consistency.csv` (`mean_fas_weight_removed` = 0.0 for all `ms2` rows and most `ms1_drop_mutual` rows) | Cross-referenced against `table_bootstrap_delta_ndcg.csv` zero-delta rows | Interpretive | High | Matches the `repair_inactive` failure class (64% of records) in `experiments/failure_class_audit_20260711_212157/phase_reports/manual_failure_summary.csv`, though that 64% figure is computed over the broader failure-mining pooled corpus (1,020 records), not identical in denominator to the 24-row bootstrap table — both point the same direction and are not in tension, but are two different corpora and are not conflated in the sentence |
| I-9 | "in the one regime where graphs are substantially cyclic, the retrieval effect of repair is heterogeneous across datasets and, in three of four cases, statistically indistinguishable from no effect once bootstrap uncertainty is taken into account" | Of the 4 `ms1`/Copeland rows, 3 have CIs that straddle zero (SciDocs, FiQA, BRIGHT) | `table_bootstrap_delta_ndcg.csv` rows `{scidocs,fiqa,bright} × ms1 × copeland`; classification cross-checked in `figure4_bootstrap_data.csv` (`ci_relation_to_zero = straddles_zero` for these 3 rows) | 3 of 4 `ms1` copeland rows | Inferential | High | None |
| I-10 | "a single dataset shows a reliable positive effect, and it is not the dataset with the most severe structural inconsistency" | HotpotQA ms1 Copeland is the only row with a CI bounded away from negative (mean Δ = +0.0167, CI [0, 0.0405]); HotpotQA has the *lowest* ms1 cyclicity (51.92%) of the four datasets | `table_bootstrap_delta_ndcg.csv` row `hotpotqa,ms1,copeland`; `table_graph_ndcg_and_consistency.csv` `pct_cyclic` column (FiQA 95.0 > SciDocs 87.5 > BRIGHT 60.0 > HotpotQA 51.92) | Single row + cross-dataset ranking comparison | Inferential | High | "Reliable positive effect" here means `ci_relation_to_zero = positive_bounded_at_zero` (CI lower bound is exactly 0.0, not strictly positive) — see the careful "does not cross zero below" convention documented in `papers/JDIQ_2026/figure4_evidence/FINAL_REPORT.md`; the Introduction avoids the stronger phrase "excludes zero" for this reason |
| I-11 | "we additionally test a bounded, cross-dataset sample of real large-language-model pairwise, pointwise, and listwise judgments" | Real-LLM pilot exists across SciDocs, HotpotQA, FiQA | `outputs/openai_real_llm_cross_dataset_summary.md`; `outputs/openai_scidocs_real_pairwise_q50_k15/`, `outputs/openai_hotpotqa_real_run_q20_k15/`, `outputs/openai_fiqa_real_run_q20_k15/` | N/A (narrative summary + per-run dirs) | Descriptive | High | Query counts are small (SciDocs 50q, HotpotQA 20q, FiQA 10 processed queries per `docs/related_work_positioning_note.md` §1) — the Introduction deliberately says "bounded" and "a supporting check rather than a confirmatory result," consistent with `CANONICAL_PAPER_STORY.md`'s prohibited-claims table ("Real-LLM results confirm generalization" → unsupported) |
| I-12 | "the greedy feedback-arc-set heuristic used in the main experiments is compared against stronger exact and near-exact repair variants, which improve the graph-internal structural objective further but do not change the retrieval conclusion" | Exact/stronger repair improves structural objective but not retrieval | `experiments/final_method_gap_audit_20260711_221113/task2/repair_comparison_real.csv` | Rows `exact_scc_dp20`, `exact_small_greedy_hybrid` vs. `greedy`; `vs_greedy_mean_delta_copeland` column (≈ 0, e.g. `exact_small_greedy_hybrid` = −3.58e-05) | Descriptive/inferential | High | Matches claim-matrix classification `exact_beats_greedy_structural = safe_with_qualification` and `stronger_repair_improves_retrieval = unsupported` in `experiments/publication_readiness_audit_20260711_233629/final_claim_support_matrix.csv` |
| I-13 | "we additionally evaluate the repaired hybrid rankings against a broad grid of fixed aggregation baselines, including reciprocal rank fusion and CombSUM" | Pooled baseline grid exists and includes RRF, CombSUM | `experiments/final_method_gap_audit_20260711_221113/task3/final_baseline_comparison.csv` | Rows `scope=pooled`, `method` ∈ {combsum, rrf, ...} | Descriptive | High | None |
| I-13b | "we also examine directly whether that fusion step itself can mask a graph-level change, so that a null retrieval result is not silently misattributed to repair being unhelpful" | Fusion may suppress/mask repair effects; examined as a distinct hypothesis | `experiments/final_method_gap_audit_20260711_221113/task1/extraction_fusion_complete.csv`; classification `fusion_suppresses_repair` in `experiments/publication_readiness_audit_20260711_233629/final_claim_support_matrix.csv` | `task1/extraction_fusion_complete.csv` rows where `method` contains `hybrid_repaired` (`fusion_suppression_rate` column) | Descriptive (states the check is performed) / interpretive (the mechanism) | Medium | Claim matrix classifies `fusion_suppresses_repair` as `safe_with_qualification` ("supported diagnostically not as universal law") — the Introduction's wording states the study *examines* this possibility, not that fusion suppression is confirmed as *the* explanation for null results; added specifically to close a gap identified in `REVIEWER_CONCERN_COVERAGE.md` R5 |
| I-14 | "a six-class failure taxonomy that explains why repair is retrieval-inactive, neutral, or harmful in specific, interpretable terms" | 6 named failure classes exist | `experiments/failure_class_audit_20260711_212157/phase_reports/manual_failure_summary.csv` | 6 rows (`repair_inactive`, `tail_only_change`, `wrong_direction_repair`, `metric_neutral_ranking_change`, `extraction_insensitivity`, `unknown_or_mixed`) | Descriptive | High | None |
| I-15 | "1,020 query×regime evaluation records" | Total record count | `experiments/failure_class_audit_20260711_212157/phase_reports/manual_failure_summary.csv` (sum of `count` column = 1,020); confirmed in `CANONICAL_PAPER_STORY.md` line 39 | Sum of all 6 rows | Descriptive | High | None |
| I-16 | "the release of a curated benchmark resource, CARB, that packages the graph-theoretic features, repair outcomes, and ranking scores" | CARB schema/scope | `experiments/created_data_audit_20260711_232004/phase10/PROPOSED_DATASET_SCHEMA.md` | N/A (schema document) | Descriptive | Medium | CARB is a **proposed** schema/release plan, not yet a packaged public release (`papers/JDIQ_2026/TABLE_PLAN.md` Table 10 status: "audit complete," but `PROJECT_STATUS.md` CARB readiness = 35%, "feature files packaged: Not built") — the Introduction says "the release of," matching the paper's forward-looking contribution framing consistent with `CANONICAL_PAPER_STORY.md`'s central-contribution list, but manuscript authors should verify this phrasing still holds by submission time if packaging remains incomplete |
| I-17 | "where a graph-based diagnostic such as backward-edge weight is computed against the same relevance judgments used for retrieval evaluation, we flag that circularity explicitly" | BEW/PIC circularity acknowledgment | `docs/THREATS_TO_VALIDITY.md` (implied; not a numbered item verbatim but consistent with reviewer concern R12); `experiments/reviewer_response_state_audit_20260711_214959/reviewer_criticism_inventory.csv` row `R12` | N/A (methodological caveat) | Descriptive | High | This sentence commits the manuscript to disclosing the BEW/PIC circularity in the (not-yet-written) Threats to Validity section — flagged as a **forward reference**, not yet fulfilled in this task's scope |
| I-18 | "an earlier version of this analysis was reviewed and found wanting on several specific grounds that we address directly in the study design" | Reference to the rejected IJCS submission and reconstructed reviewer criticisms | `experiments/reviewer_response_state_audit_20260711_214959/REVIEWER_CRITICISM_RECONSTRUCTION.md`, `reviewer_criticism_inventory.csv` | 15 reconstructed criticisms (R1–R15) | Descriptive (contextual, not numeric) | Medium | The reconstructed criticisms are explicitly labeled `reconstructed_summary` in the source file, not verbatim reviewer quotations (no original rejection email/report was found in the repository) — the Introduction's phrasing ("found wanting on several specific grounds") is deliberately generic and does not claim to quote actual reviewers |

---

## Sentences intentionally NOT mapped (non-empirical)

The following categories of sentences in the Introduction are rhetorical, definitional, or citation-only and carry no independently checkable quantitative claim: the opening motivation paragraph (heterogeneous rankers/pipelines, general framing); the analogy to other information-quality problems (contradictory records, non-integrable schemas); the "we treat these null and mixed outcomes as part of the contribution" framing sentence; the five-item contributions list (each item is a restatement of claims already mapped above, not a new empirical assertion); and the final roadmap paragraph (structural, not empirical).

## Coverage check

All 19 sentences carrying an empirical or quantitative claim in `main.tex` §1 are mapped above (I-1 through I-18, plus I-13b added after the fusion-suppression clause was inserted to close the R5 gap identified in `REVIEWER_CONCERN_COVERAGE.md`). No empirical sentence in the Introduction is unsupported by a canonical source.
