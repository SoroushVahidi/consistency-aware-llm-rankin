# HISTORICAL — not the current canonical evidence package

Earlier, two-dataset predecessor to `outputs/pub_vote_cmp_all4/` (itself also historical — see that directory's `HISTORICAL.md`). Not referenced anywhere in the current, submitted `papers/JDIQ_2026/manuscript/main.tex`.

**Current classical-study canonical evidence** is `reports/full_calibrated_core/outputs/calibrated_all4/paper_package/tables/` — see `docs/REPRODUCTION_CANONICAL.md` at the repo root.

Kept in place because `scripts/generate_q1_tables.py` still defaults to this path.

Added 2026-07-30, repository organization Stage 2.
